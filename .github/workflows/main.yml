name: Test

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: install
      run: yarn
    - name: lint
      run: yarn lint
    - name: test
      run: yarn test
        

