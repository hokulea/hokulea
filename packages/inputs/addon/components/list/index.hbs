<ul
  role="listbox"
  local-class="list"
  ...attributes
  {{control items=@items selection=@selection select=this.select}}
>
  {{#each @items as |item|}}
    <li role="option">
      {{yield item}}
    </li>
  {{/each}}
</ul>