<ul
  role="listbox"
  local-class="list"
  aria-multiselectable={{if @multiple "true"}}
  data-test-list
  ...attributes
  {{composite
    objects=@options
    selection=@value
    select=this.select
    moveFocus=this.activateItem
  }}
>
  {{#each @options as |option|}}
    <li role="option">
      {{#if (has-block)}}
        {{yield option}}
      {{else}}
        {{option}}
      {{/if}}
    </li>
  {{/each}}
</ul>