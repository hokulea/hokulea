<ul
  role="listbox"
  local-class="list"
  aria-multiselectable={{if @multiple "true"}}
  data-test-list
  ...attributes
  {{composite
    objects=@options
    focus=this.activateItem
    selection=@value
    select=this.select
  }}
>
  {{#each @options as |option|}}
    <li role="option">
      {{#if (has-block)}}
        {{yield option}}
      {{else}}
        {{option}}
      {{/if}}
    </li>
  {{/each}}
</ul>