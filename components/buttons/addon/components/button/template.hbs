<ButtonBuilder local-class="button" ...attributes as |builder|>
  {{#if (has-block-params)}}
    {{yield builder}}
  {{else}}
    <builder.Content>{{yield}}</builder.Content>
  {{/if}}
</ButtonBuilder>