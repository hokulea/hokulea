{
  "name": "@hokulea/tokens",
  "version": "0.0.0",
  "description": "Design Tokens for hokulea",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf build/ tokens/",
    "sync": "yarn theemo sync",
    "build": "yarn style-dictionary build --config sd.js",
    "magic": "npm-run-all sync build",
    "clean-magic": "yarn clean && yarn magic"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "style-dictionary": "^3.0.2",
    "theemo": "^0.1.2"
  },
  "volta": {
    "extends": "../../package.json"
  }
}
